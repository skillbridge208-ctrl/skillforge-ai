# SkillForge AI – Intelligent Career Path Builder

CLI app that generates and updates personalized career roadmaps using Gemini + Firebase.

## Run
1. Activate venv  
   `.\venv\Scripts\Activate.ps1`
2. Install packages  
   `pip install -r requirements.txt`
3. Start  
   `python app.py`
